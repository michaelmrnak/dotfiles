# =============================================================================
# Brewfile
# Run: brew bundle
# Update lockfile: brew bundle dump --force
# =============================================================================

# Taps
tap "homebrew/bundle"

# -----------------------------------------------------------------------------
# Core CLI Tools
# -----------------------------------------------------------------------------
brew "git"
brew "chezmoi"           # dotfiles manager
brew "mise"              # universal version manager (node, python, ruby, go, rust, etc.)
brew "direnv"            # per-directory env vars (great for API keys)
brew "gh"                # GitHub CLI
brew "jq"                # JSON processor
brew "ripgrep"           # fast grep (rg)
brew "fzf"               # fuzzy finder
brew "bat"               # better cat
brew "eza"               # better ls
brew "zoxide"            # smarter cd

# -----------------------------------------------------------------------------
# Development
# -----------------------------------------------------------------------------
brew "git-delta"         # better git diffs

# -----------------------------------------------------------------------------
# Apps (Casks)
# -----------------------------------------------------------------------------
cask "warp"              # terminal
cask "visual-studio-code"

# Optional — uncomment what you want:
# cask "arc"             # browser
# cask "1password"       # password manager
# cask "1password-cli"   # 1password CLI (great for secrets in chezmoi)
# cask "tableplus"       # database GUI
# cask "figma"
